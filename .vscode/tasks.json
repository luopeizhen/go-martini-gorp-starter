{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "server",
            "type": "shell",
            "command": "$env:GOPATH=\\\"${workspaceFolder};$env:GOPATH\\\";go install server;",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}